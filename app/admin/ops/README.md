# Delivery operations

Open `/admin/ops` after signing in to Admin. This control room manages the client-facing delivery state and project message while keeping storage and access controls in the main Admin area.

Before using the new delivery fields, run `supabase/migrations/005_phase5_project_workflow.sql` in the Supabase SQL Editor.
